### This file was automatically generated. Don't edit it!
binary_sensor:
- name: social/is_vacuuming_day_time
  unique_id: binary_sensor.social_is_vacuuming_day_time
  state: >-
    {{
      (
        is_state('binary_sensor.social_workday', 'on')
      and
        (
           (
             (today_at(states('input_datetime.social_vacuuming_workday_start_day')) < today_at(states('input_datetime.social_vacuuming_workday_start_night')))
           and
             (today_at(states('input_datetime.social_vacuuming_workday_start_day')) < now())
           and
             (now() < today_at(states('input_datetime.social_vacuuming_workday_start_night')))
           )
        or
           (
             (today_at(states('input_datetime.social_vacuuming_workday_start_day')) > today_at(states('input_datetime.social_vacuuming_workday_start_night')))
           and
             (today_at(states('input_datetime.social_vacuuming_workday_start_day')) < now())
           )
        or
           (
             (today_at(states('input_datetime.social_vacuuming_workday_start_day')) > today_at(states('input_datetime.social_vacuuming_workday_start_night')))
           and
             (now() < today_at(states('input_datetime.social_vacuuming_workday_start_night')))
           )
        )
      )
    or
      (
        is_state('binary_sensor.social_workday', 'off')
      and
        (
           (
             (today_at(states('input_datetime.social_vacuuming_holiday_start_day')) < today_at(states('input_datetime.social_vacuuming_holiday_start_night')))
           and
             (today_at(states('input_datetime.social_vacuuming_holiday_start_day')) < now())
           and
             (now() < today_at(states('input_datetime.social_vacuuming_holiday_start_night')))
           )
        or
           (
             (today_at(states('input_datetime.social_vacuuming_holiday_start_day')) > today_at(states('input_datetime.social_vacuuming_holiday_start_night')))
           and
             (today_at(states('input_datetime.social_vacuuming_holiday_start_day')) < now())
           )
        or
           (
             (today_at(states('input_datetime.social_vacuuming_holiday_start_day')) > today_at(states('input_datetime.social_vacuuming_holiday_start_night')))
           and
             (now() < today_at(states('input_datetime.social_vacuuming_holiday_start_night')))
           )
        )
      )
    }}
