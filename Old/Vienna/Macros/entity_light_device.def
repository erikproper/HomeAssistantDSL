function entity_light_device {
  if [ "$2" == "" ]; then
    LightSphere="social"
    LightName="$1"
  else
    LightSphere="$1"
    LightName="$2"
  fi
 
  entity declare light.$LightSphere:$LightName
    provides device_node $LightName/light
}